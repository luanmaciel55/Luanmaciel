'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';import {supabase} from '../../lib/supabase';export default function Entrar(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[msg,setMsg]=useState('');const router=useRouter();async function entrar(e:React.FormEvent){e.preventDefault();setMsg('Entrando...');const{error}=await supabase.auth.signInWithPassword({email,password});if(error){setMsg('E-mail ou senha inválidos, ou a conta administrativa ainda não foi criada.');return}const{data}=await supabase.rpc('is_site_admin');if(!data){await supabase.auth.signOut();setMsg('Esta conta não possui acesso administrativo.');return}router.push('/admin');router.refresh()}return <section className="section"><h1>Entrar</h1><p>Acesso reservado ao administrador.</p><form onSubmit={entrar} className="adminform"><label>E-mail<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Senha<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><button className="cta" type="submit">Entrar</button>{msg&&<p>{msg}</p>}</form></section>}