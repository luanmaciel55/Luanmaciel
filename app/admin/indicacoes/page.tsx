'use client';import{useEffect,useState}from'react';import Link from'next/link';import{supabase}from'../../../lib/supabase';
export default function GerenciarIndicacoes(){const[ok,setOk]=useState<boolean|null>(null),[items,setItems]=useState<any[]>([]);useEffect(()=>{(async()=>{const{data:{user}}=await supabase.auth.getUser();if(!user){setOk(false);return}const{data:a}=await supabase.rpc('is_site_admin');setOk(!!a);if(a){const{data}=await supabase.from('recommendations').select('*').order('sort_order');setItems(data||[])}})()},[]);if(ok===null)return <section className="section"><p>Carregando...</p></section>;if(!ok)return <section className="section"><h1>Acesso restrito</h1></section>;return <section className="section recManager"><div className="recManagerHead"><div><span className="eyebrow">PAINEL DO ADMINISTRADOR</span><h1>Indicações</h1><p>Gerencie todas as pessoas, serviços e produtos que você indicar.</p></div><button className="cta" disabled title="O formulário de nova indicação será aberto aqui">+ Nova indicação</button></div><div className="recAdminList">{items.map((x,i)=><article className="recAdminCard" key={x.slug}><div className="recAdminNumber">{String(i+1).padStart(2,'0')}</div><div className="recAdminInfo"><small>INDICAÇÃO</small><h2>{x.card_title}</h2><p>{x.card_description}</p><span className={x.active?'recStatus active':'recStatus'}>{x.active?'Publicada':'Oculta'}</span></div><div className="recAdminActions"><Link className="cta" href={'/admin/indicacoes/'+x.slug}>Editar indicação</Link><Link className="viewBtn" href={'/indicacoes/'+x.slug}>Ver página</Link></div></article>)}</div>{items.length===0&&<div className="recEmpty"><h2>Nenhuma indicação ainda</h2><p>Quando você adicionar indicações, elas aparecerão aqui uma embaixo da outra.</p></div>}</section>}