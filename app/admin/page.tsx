'use client';import {useEffect,useState} from 'react';import Link from 'next/link';import {supabase} from '../../lib/supabase';export default function Admin(){const[ok,setOk]=useState<boolean|null>(null);useEffect(()=>{(async()=>{const{data:{user}}=await supabase.auth.getUser();if(!user){setOk(false);return}const{data}=await supabase.rpc('is_site_admin');setOk(!!data)})()},[]);if(ok===null)return <section className="section"><p>Verificando acesso...</p></section>;if(!ok)return <section className="section"><h1>Acesso restrito</h1><p>Entre com a conta de administrador.</p></section>;return <section className="section"><h1>Painel do administrador</h1><p>Gerencie o site por áreas.</p><div className="cards"><div className="card"><h2>Análises</h2><p>Hoje • 7 dias • mês • ano • sempre</p></div><div className="card"><h2>Editar site</h2><p>Informações, páginas e contato.</p></div><div className="card"><h2>Hotsite /aqui</h2><p>Gerencie os botões e a aparência da página especial.</p><div className="adminActions"><Link className="cta" href="/editar-hotsite">Editar Hotsite</Link><Link className="viewBtn" href="/aqui">Ver</Link></div></div></div></section>}