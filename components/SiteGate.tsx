'use client';import{useEffect,useState}from'react';import{usePathname}from'next/navigation';export default function SiteGate({children}:{children:React.ReactNode}){const path=usePathname();const publicPage=path==='/aqui'||path.startsWith('/aqui/')||path==='/indicacoes'||path.startsWith('/indicacoes/');const[open,setOpen]=useState(false),[pass,setPass]=useState(''),[msg,setMsg]=useState('');useEffect(()=>{if(publicPage){setOpen(true);return}setOpen(sessionStorage.getItem('site-unlocked')==='1')},[path,publicPage]);if(publicPage)return <>{children}</>;if(open)return <>{children}</>;async function go(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/site-lock',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({password:pass})});if(r.ok){sessionStorage.setItem('site-unlocked','1');setOpen(true)}else setMsg('Senha incorreta.')}return <main className="lockScreen"><form onSubmit={go} className="lockCard"><h1>Site em preparação</h1><p>Digite a senha para acessar.</p><input type="password" value={pass} onChange={e=>setPass(e.target.value)} required/><button className="cta">Acessar</button>{msg&&<p>{msg}</p>}</form></main>}